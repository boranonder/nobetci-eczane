import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Uygulama başlatma testi', (WidgetTester tester) async {
    // Temel smoke test — gerçek testler servislerin mock'lanmasını gerektirir
    expect(true, isTrue);
  });
}
